[phases.setup]
nixPkgs = ["python311", "gcc", "tesseract"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt --no-cache-dir"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
